# Normalize all text files to LF in the repo (stops the CRLF warnings on Windows,
# keeps diffs clean across Windows / macOS / Linux).
* text=auto eol=lf

# Treat these as binary: never normalize or try to diff/merge them.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
