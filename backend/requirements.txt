# Core Web Framework
fastapi==0.109.0
uvicorn==0.27.0.post1  # Used via command line

# Database
sqlalchemy==2.0.36  # Updated for Python 3.13 compatibility
alembic==1.14.0  # Updated for Python 3.13 compatibility
psycopg2-binary>=2.9.9  # Used implicitly by SQLAlchemy

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.0  # Pin to version compatible with passlib 1.7.4
python-multipart==0.0.9  # Used implicitly by FastAPI
# PyJWT==2.8.0  # UNUSED - Functionality covered by python-jose

# Environment & Configuration
python-dotenv==1.0.0
pydantic==2.10.3  # Updated for Python 3.13 compatibility
pydantic-settings==2.6.1  # Updated for compatibility with pydantic 2.10
email-validator==2.1.1  # Used implicitly by Pydantic EmailStr

# AWS Integration
boto3==1.34.28

# Data Processing
numpy==1.26.4
pandas==2.2.3  # Updated for Python 3.13 compatibility (has pre-built wheels)
scikit-learn==1.5.2  # Updated for Python 3.13 compatibility

# Image Processing - MISSING PACKAGES NEEDED
# Pillow==10.2.0  # UNUSED - No PIL/Image imports found
opencv-python==4.10.0.84  # MISSING - cv2 used in floor_plan_analyzer.py, updated for Python 3.13 compatibility
scipy==1.14.1  # MISSING - Used in floor_plan_analyzer.py, updated for Python 3.13 compatibility

# API & Documentation
# httpx==0.27.0  # UNUSED - No imports found
# structlog==23.1.0  # UNUSED - No imports found

# OpenAI Integration
openai>=1.30.0

# Web Server
gunicorn==21.2.0  # Used via command line

# CORS Handling - FastAPI has built-in CORS
# Flask-Cors==4.0.0  # UNUSED - FastAPI uses CORSMiddleware

# Additional Utilities
# asyncio==3.4.3  # UNUSED - Built into Python 3.7+

# Task Scheduling
schedule==1.2.0

# HTTP Requests
requests==2.31.0

# Machine Learning & Embeddings
# Note: PyTorch CPU version installed separately in Dockerfile (see docker/backend/Dockerfile)
# This avoids the large CUDA dependencies (~700MB smaller)
sentence-transformers==2.7.0  # For vector embeddings and semantic search
# accelerate>=0.20.0  # UNUSED - No imports found

